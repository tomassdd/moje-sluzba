version: "3"

services:
  mojeapi:
    build: .
    ports:
      - "8081:8081"
  db:
    image: postgres:16-alpine
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=milujureneho
      - POSTGRES_DB=studydb
